# 세션 리포트 2026-07-06 (_3)

## 목표 (세션 시작 시 선언한 것)
원격·GUI 제약 하에서 진행 가능한 트랙 착수 — SIM-불필요 트랙(로그 스키마 인벤토리) 시작 + 정합/결정 소작업.

## 완료
- **로그 스키마 인벤토리 (V7.5)** — `runbooks/log_schema_inventory.md`. 기존 Stage 03 Built-in 실행 로그
  3회분(재실행 불필요)에서 eventlog/statelog/result **실제 컬럼 실측 확정**:
  - statelog(22컬럼)에 설계노트 **지표 6종 전부 존재**(TTC/VtV/WayOff/AccelX,Y/Exceeding·Deficit Speed) → Metric 정본 확증(Decision 5).
  - eventlog `Storyboard started/stopped` = 시나리오 경계(Stage 03.7).
  - result verdict = 실측 전부 `NONE`(평가기준 미트리거). 빈 로그 = 무효 실행 시그니처.
- **AVS-008 RESOLVED** — `~/avstack/STATUS.md`([WS], 비git)의 stale 자체 판정(Stage 0~8, 01~08 TODO ↔ stages.tsv 01~04 PASS)을
  **stages.tsv 포인터로 전환**. 판정 이중화 제거, 스테이지 체계 00~05 일원화.
- **ADR-009 기록 + 설계노트 erratum 2건** — E안(무인 데이터 경로 = Runner 산출물+API polling, rosbag=유인,
  L6에서 ROS2 Native 제외). 설계노트 §5.8(ROS2 Native 실측 반증, D1=FAIL)·§9.4(무인/유인 구분) erratum.
  PROJECT_STATUS Erratum Queue 5.8/9.4 해소, ADR-009 확정 승격.

## 미완 + 이유
- **parser 골격·golden fixture**: [EVAL] avstack-eval 리포 생성이 전제(경계상 [CONTROL]에 프레임워크 코드 금지). 리포 생성 여부 결정 대기.
- **시나리오 다양화·실 PASS/FAIL 검증**: SIM 실행(GUI) 필요 — 현재 원격·GUI 제약으로 보류. 스키마 확정까지가 이번 산출.
- **MORAI 회신**(~7/10), **MORAI-003 초안**, **Risk Register(R16/17/18) 등재**: 대기.

## 신규: 이슈 / 결정 / 벤더 커뮤니케이션 (TSV 반영 여부 체크)
- 이슈: **AVS-008 RESOLVED**(이번 세션). 신규 OPEN 없음. issues.tsv ✓.
- 결정: **ADR-009 ACCEPTED**(E안). decisions.tsv ✓.
- 벤더: 변화 없음(001/002/004 SENT, 회신 대기). commands.tsv에 스키마 인벤토리 1행 ✓.

## 교훈 (CLAUDE.md 규칙 승격 후보 표시)
- **재생성물 재활용**: "GUI로 로그 확보"가 전제였으나 **기존 실행 로그가 이미 [WS]에 존재** → SIM 재실행 없이 스키마 인벤토리 완주.
  원격·GUI 제약에서도 [WS] 기존 산출물로 진행 가능한 작업을 먼저 찾는다.
- 전략 결정(ADR)·정본(설계노트) 편집은 초안을 보여주고 확정 — 이번에도 ADR-009/erratum 초안 확인 후 진행.

## 다음 세션 필수 작업 (PROJECT_STATUS 7장에 반영했는지 체크) — 반영 ✓
1. MORAI 회신 추적(~7/10) — 회신 시 INBOX + ANSWERED 전이.
2. [EVAL] 리포 착수 검토 — log_schema_inventory 기반 parser 골격·golden fixture.
3. Risk Register(R16/17/18) 등재(Erratum Queue 잔여), MORAI-003 초안.
