# 세션 리포트 2026-07-10

## 목표 (세션 시작 시 선언한 것)
T-24 실행 — 벤더 진단(2026-07-08 "morai_msgs 미소싱") 검증: SIM 기동 가능 세션에서 2×2 매트릭스의 미실행 2셀(N2/R2) 실측 완주 + 분기 판정 + 후속 조치.

## 완료
- **T-24 매트릭스 완주 — 분기 C(벤더 진단 틀림)**:
  - **N2**(native + 26.R1 오버레이 소싱): 소싱 실물 증거(`/proc` environ, `AMENT_PREFIX_PATH`에 ros2_ws_26r1) 상태에서
    startup `std::bad_cast` **동일 재현**(Player.log:547, rcl/rmw 초기화 계층), D1 74xx 미바인딩. 기존과 동일 → r1 종결.
  - **R2**(rosbridge + 26.R1 정합 msgs): 연결(client 33)·토픽 23종 생성 정상이나 수신 0건.
    `header.seq` 거부 **138,841건**(15분 누계, 9타입 — **표준 tf2_msgs 포함** = msgs 소싱·버전 무관 증명).
    역방향 CtrlCmd 53건 발행에도 차량 무반응(사용자 육안).
- **기록**: commands.tsv 2행, issues.tsv AVS-007 AMEND, runbook v1.1(§3 표·§4 판정), CLAUDE.md·PROJECT_STATUS 갱신.
- **Erratum**: 거부건수 53,686(observe 11:41 스냅샷) ↔ 동결 증거 138,841(15분 누계) 불일치 발견(사용자 재검토 지적) → 문서 일괄 통일(f977f11).
- **재회신 발송 완료(SENT)**: 지난번 커버 구조로 2문서 분리(이메일 본문 + 기술상세), 발송 전 재검토(주장-증거 전건 대조·rosbridge 2.0.7 dpkg 실측·민감정보 0건),
  send 패키지(본문 send본·기술상세 PDF·evidence zip 4건) 동결, 발송 후 SENT/20260710_* 동결 + vendor_comms SENT 전이.
- **GUI 절차 실측 정정**: 런처 → **Start 먼저** → 지도/차량 선택(기존 문서 순서 반대). SIM Network 3종은 자동 연결돼 있었음(수동 Connect 불요).

## 미완 + 이유
- **AVS-006 재빌드 API 수령**: 벤더 목표일 = 오늘(7/10). 세션 종료 시점 미수령 — 재회신 이메일에 리마인드 문단 포함해 발송함.
- **[MGMT] 반영**: SIM-05 판정/TECH-03·14·25 변경안/kb 요약 — 체크리스트는 채팅으로 전달, oss3-mgmt 반영은 사용자 작업.
- **MORAI-003 초안, Risk Register R16/17/18, [EVAL] 리포 결정**: 이월.
- t24_observe.sh의 정수비교 경고(grep -c 다중행 출력, line 84/101) — 판정에 영향 없어 미수정. 재사용 시 정리.

## 신규: 이슈 / 결정 / 벤더 커뮤니케이션 (TSV 반영 여부 체크)
- 이슈: AVS-007 AMEND 1행(T-24 반증, 분기 C). 신규 OPEN 없음. issues.tsv ✓.
- 결정: 신규 ADR 없음.
- 벤더: MORAI-001 재회신 DRAFT→(분리)→send-ready→**SENT**(AMEND 4행). vendor_comms.tsv ✓. commands.tsv 2행 ✓.

## 교훈 (CLAUDE.md 규칙 승격 후보 표시)
- **관측 시점 명시**: 누적 카운터(로그 거부 건수 등)는 스냅샷 시각과 함께 기록하지 않으면 동결 증거와 모순이 생긴다.
  대외 문서는 **동결본 기준 수치**로 통일하고, TSV에는 관측 시각을 함께 남긴다. (이번 erratum의 원인)
- **증거는 대상 프로세스의 실물로**: "소싱했다"의 증거는 셸이 아니라 **대상 프로세스의 `/proc/<pid>/environ`** — 이번에 벤더 반증의 핵심 증거가 됨.
- 벤더 진단이 기존 실측과 배치돼도 **미실측 셀이 있으면 기각하지 말고 실측**한다(N2는 실제로 사각지대였음 — 결과는 반증이지만 이제 증거가 완전).

## 다음 세션 필수 작업
1. AVS-006 재빌드 API 수령 확인 → 새 conda env 재생성(yml 커밋) → **Stage 03.5 재개** (1순위)
2. MORAI-001 재회신 회신 추적(INBOX 저장 + ANSWERED 전이) — 답변 3건에 따라 Stage 05 경로 결정
3. [MGMT] 체크리스트 반영 확인, MORAI-003 초안, [EVAL] 리포 결정
