# 세션 리포트 2026-07-10

## 목표 (세션 시작 시 선언한 것)
T-24 실행 — 벤더 진단(2026-07-08 "morai_msgs 미소싱") 검증: 매트릭스 미실행 2셀(N2/R2) 실측 + 분기 판정 + 후속 조치.
(세션 중 벤더 2차 회신 수신으로 T-25까지 확장)

## 완료

**전반부 — T-24 (벤더 1차 진단 검증)**
- N2(native+26.R1 소싱, environ 실물 증거): std::bad_cast 동일 재현. R2(rosbridge+26.R1 msgs): header.seq 거부
  138,841건(15분 누계, 표준 tf2 포함)·수신 0·역방향 무반응 → **분기 C(진단 틀림)**, 재회신(이메일+기술상세 PDF+zip) 발송·SENT 동결.
- Erratum: 거부건수 53,686(observe 스냅샷)↔138,841(동결 누계) 불일치 발견(사용자 지적) → 일괄 통일.

**후반부 — T-25 (벤더 2차 회신 검증 → AVS-007 RESOLVED, Stage 05 PASS)**
- 벤더 2차 회신: 자기 환경(fastdds 2.6.10/rmw 6.2.8), **"소싱 후 바이너리 직접 실행" 재확인 요청**,
  rosbridge=ROS2 공식 경로 아님, **API py3.13 재빌드 납품**.
- **N3(벤더 방식)**: SIM 생존 → ROS2 Connect → 토픽 18종(`morai_ros2_msgs` 타입)·**/ego_vehicle_status 50.2Hz 수신**.
- **N3r2 3국면 판별**(교란변수 분리): A. 무명령 크리프=중력 롤링(0.4→0.6m/s) / B. **설정창 열림=물리 일시정지**
  (10s 위치 완전 동결, 토픽은 마지막 값 반복 — 그간 무반응 혼선의 원인) / C. 명령-움직임 시간상관 —
  velocity 10km/h→1.5s 내 2.778m/s 추종, stop→1s 내 0·위치 고정, 반복 재현 → **역방향 제어 확정**.
- **N4(H4 확정)**: 벤더 방식 + `RMW_IMPLEMENTATION=rmw_fastrtps_cpp`만 추가 → 동일 크래시(Player.log:547) 재현.
  → **근본원인 = RMW_IMPLEMENTATION 환경변수**(래퍼가 항상 설정). 소싱·msgs 버전·host 패치버전 무관.
- 일괄 정정: 래퍼 SOURCE_ROS2 블록(RMW 미설정+unset, 오버레이 기본 소싱), CLAUDE.md, avs-007 리포트 erratum,
  t24 runbook v2.0(§6 T-25·동작 레시피). **stages.tsv 05 PASS, issues.tsv AVS-007 RESOLVED**(스크립트 기록).
- 벤더 3차 회신(해결 보고) 초안: OUTBOX/MORAI-001_t25_resolution_email.md (검토 대기).
- t24_observe.sh N3 지원+정수비교 버그 수정, t25_ctrl_verify.sh 신규(국면별 판별 스크립트).

**종반부 — AVS-006 RESOLVED**
- py3.13 재빌드 zip 수령(scp, md5 4c175502) → `morai-osc-py313` env(conda-forge py3.13.14 — 기본채널 ToS/상용약관 회피)
  → **sourcedefender 16.0.65 설치·`.pye` 252개 import 성공**. API 표면 확인: Importer(import_open_scenario/clear),
  Client(start·stop_scenario, get_stop_status, callbacks — 03.7 경계 감지 재료). gRPC 127.0.0.1:7789 = SIM "GRPC" 네트워크.
- environment-morai-osc-py313.yml 커밋(재현성). 3차 회신 초안에 API 검증 성공 포함(두 이슈 한 통).

## 미완 + 이유
- **Stage 03.5 SIM 연동 실행**: import·계약 표면까지 완료 — SIM GRPC Connect 상태의 실행 검증은 다음 GUI 세션.
- **3차 회신 발송**: 초안(해결 보고 2건 통합) 사용자 검토 대기.
- **ADR-009 재검토**: Stage 05 개통으로 "L6에서 ROS2 Native 제외" 전제 재검토 필요 — 다음 세션.
- MORAI-003 초안, Risk Register R16/17/18, [MGMT] 반영, [EVAL] 리포 결정: 이월.

## 신규: 이슈 / 결정 / 벤더 커뮤니케이션 (TSV 반영 여부 체크)
- Stage: **05 PASS** ✓(stages.tsv). 이슈: **AVS-007 RESOLVED** ✓(AMEND 2행: 분기C→RESOLVED). commands.tsv 4행 ✓.
- 벤더: MORAI-001 재회신 SENT→2차 회신 ANSWERED→3차 DRAFT, MORAI-002 py3.13 납품 ANSWERED ✓(vendor_comms).

## 교훈 (CLAUDE.md 규칙 승격 후보 표시)
- **최소 차이 실험의 위력**: 벤더 방식(동작)과 우리 방식(크래시)의 차이를 변수 하나씩 좁혀 1회 재기동으로 H4 확정.
  "동작하는 조건"을 확보한 뒤 역방향으로 좁히는 것이 가설 나열보다 빠르다.
- **GUI 상태는 물리에 개입한다**: SIM 설정창이 열려 있으면 물리 일시정지·토픽은 마지막 값 반복 → 원격 관측만으로는
  "무반응"과 구분 불가. GUI 개입 실험은 반드시 "창 전부 닫힘"을 전제 조건으로 명시할 것. (승격 후보)
- **판별 프로토콜 설계**: 교란변수(중력/동결/래치)가 얽히면 개별 국면(무명령 기준선→교란 재현→명령 상관)으로 분리해
  같은 트랙 로그에 타임스탬프 마커로 남긴다 — t25_ctrl_verify.sh 패턴 재사용 가치.
- 관측 시점 명시(누적 카운터), 대상 프로세스 environ이 소싱 증거, 미실측 셀은 기각 말고 실측 — 전반부 교훈 유지.

## 다음 세션 필수 작업
1. **Stage 03.5 실행**: SIM 기동+GRPC Connect(7789) → example xosc를 API로 실행 → 계약 검증 → 03.7 진입
2. MORAI-001 3차 회신(해결 보고 2건 통합) 발송 → SENT 동결·전이
3. ADR-009 재검토(ROS2 Native 개통 반영), [MGMT] 반영(SIM-05=해결, TECH-03/14/25 갱신), 구 py3.7 env 폐기
