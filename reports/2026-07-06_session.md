# 세션 리포트 2026-07-06

## 목표 (세션 시작 시 선언한 것)
재검증 E1~E3 완주 + 문의문 확정.

## 완료
- **재검증 프로토콜 배치·스크립트**: `runbooks/avs-006_007_recheck_protocol.md` 커밋, `scripts/recheck/{e2,e3}_observe.sh` 작성(E2는 sudo tcpdump→ss 포트폴링으로 전환).
- **E1** (sourcedefender, 자율 3회) = **PASS**: 결정론적 설치 실패. PyPI 실측 갱신 — **9개** 릴리스(`16.0.65` 신규 포함), 전부 >=3.9/3.10, 3.7 호환 0.
- **E2** (ROS2 Native, 인터리브 3회) = **PASS**: `SOURCE_ROS2=1` 기동 시 SIM이 startup `librmw_fastrtps_cpp std::bad_cast`로 종료. **D1=FAIL 확정**(RTPS 74xx 미바인딩=participant 미생성). ('Connect 크래시'는 측정 불가 → 소싱 startup 크래시로 적응, 사용자 승인).
- **E3** (rosbridge, 인터리브 3회) = **PASS**: 연결·토픽 생성 OK, `/Ego_topic` echo 0건, `header.seq` 거부 수만 건(8개 토픽 타입).
- **문의문 확정**: MORAI-001 §3-A/B·§4-A, MORAI-002 §3/§4를 실측(3회, 2026-07-06)으로 갱신. **재확인에서 §1 요약 2건이 §3/§4와 불일치**(001 "Connect 시"→"startup", 002 "8개"→"9개")를 잡아 정합.
- **증거 동결**(§4.2): 대표본(run1) + PyPI 스냅샷 → `vendor/morai/evidence/`(rosbridge 로그는 대표 샘플로 축약).
- **원장**: commands.tsv 실험 3행, issues.tsv AVS-007 AMEND(D1=FAIL), vendor_comms AMEND(001/002 발송준비).
- **진단 매트릭스**(avs-007_layer_diagnosis.md §5): D1=FAIL 확정 → D2~D4 불필요 종결, 귀속=벤더(ros2cs 미동작).
- PROJECT_STATUS 2·6·7장 갱신(스냅샷 2026-07-06).

## 미완 + 이유
- **문의 발송 자체**: 사용자 액션(발송 준비 완료). SENT 동결·전이 행은 **발송 후 별도 세션**.
- **D5(제어 역방향)**: D1=FAIL로 D2~D4 종결, D5만 별도 가치 판단으로 남김(미실행).
- **MORAI-003 초안 / SIM-불필요 트랙 / ADR-009**: 대기.

## 신규: 이슈 / 결정 / 벤더 커뮤니케이션 (TSV 반영 여부 체크)
- 이슈: 신규 없음. **AVS-007 AMEND**(D1=FAIL 확정) 반영 ✓.
- 결정: ADR 신규 확정 없음.
- 벤더: vendor_comms AMEND 행 — MORAI-001/002 "발송 준비 완료(N=3)", MORAI-003 subject. 반영 ✓. commands.tsv 3행 ✓.

## 교훈 (CLAUDE.md 규칙 승격 후보 표시)
- **[승격 후보] 실측 갱신 시 §1 요약까지 동기화**: §3/§4만 고치고 §1을 안 고쳐 불일치가 남았음 — 발송 전 재확인으로 포착. "문의문 실측 갱신은 요약 절 정합까지 1세트."
- **[재사용 패턴] sudo 비대화형 실패 → `ss` 포트 폴링**: 백그라운드 sudo tcpdump는 암호를 못 받아 실패. `ss`(자기 프로세스, no-sudo)로 "RTPS 포트 미바인딩" 측정이 오히려 더 깨끗한 D1 증거.
- 프로토콜 하드코딩 날짜(20260705) vs 실제 실행일(20260706) 정정 — 증거는 실행일 기준.

## 다음 세션 필수 작업 (PROJECT_STATUS 7장에 반영했는지 체크) — 반영 ✓
1. MORAI-001/002 발송(제목 내부 ID + INTERNAL 제거) → 발송 직후 별도 세션 SENT 동결 + vendor_comms SENT 전이 행.
2. SIM-불필요 트랙 착수(eventlog/statelog 스키마 → parser 골격).
3. MORAI-003 초안 / ADR-009(E안) 등재 + 설계노트 erratum.
