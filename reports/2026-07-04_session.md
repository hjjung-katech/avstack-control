# 세션 리포트 2026-07-04

## 목표 (세션 시작 시 선언한 것)
- AVS-007 계층 분리 진단(D1~D5) 계획을 저장소에 반영하고 실행 스크립트를 스캐폴딩.
- (원격 상황) 진단을 Claude가 자율 수행 시도.
- 문서·벤더 커뮤니케이션 관리 체계(PROJECT_STATUS + doc_and_vendor_management)를 저장소에 적용.

## 완료
- **AVS-007 진단 스캐폴딩**: `runbooks/avs-007_layer_diagnosis.md`(부록 A 정합 노트 포함) + `scripts/avs007_diag/`(preflight, d1~d4, run_all) — 문법 검증 완료.
- **원격 자율 수행 시도 → 한계 규명**: SIM 설정(`prefs`)에 ROS 네트워크 설정 비저장(gRPCPort만) 확인; 런처는 Simulator 자동기동 안 함; GUI 자동화 도구(xdotool/wmctrl/Xlib)·화면캡처(PNG) 부재로 Network Settings Connect 불가 → 진단은 사람 1분 GUI 세팅 필요. 잔재 talker(RTPS 오염원) 정리.
- **문서·벤더 관리 체계 적용**(커밋 a~d): PROJECT_STATUS.md(루트) + doc_and_vendor_management.md **v1.1**; `reports/`·`vendor/morai/{OUTBOX,SENT,INBOX,templates,evidence}` 신설; `records/vendor_comms.tsv`·`commands.tsv` 생성; 파일 이관(문의초안→OUTBOX/MORAI-001, 회고→reports/2026-07-03_session.md); 템플릿 2종; CLAUDE.md에 PROJECT_STATUS 시작/종료 규약 1줄.
- **MORAI-001**(AVS-007): 템플릿 6절 재정렬 → §1 요약·§3 재현 절차 보강(§4 사실만) → avs007 증거 `vendor/morai/evidence/` 동결.
- **MORAI-002**(AVS-006): 신규 초안(AVS-006 원장·실측만). 발송품질 정정 A~D(자기모순 제거, 개발사 문서 명시, INTERNAL 주석, 증거 동결).
- **environment-morai-osc.yml** conda export·커밋(재현성 부채 청산).
- **PROJECT_STATUS 첫 정기 갱신**(2·3·5·6·7장).

## 미완 + 이유
- **MORAI-001/002 발송**: 사용자 액션(§3 N회 기입 후 발송) — 원격이라 미발송.
- **D1~D5 진단 완주**: GUI 조작 수단 부재로 원격 불가 → PC 접근 시 사람 1분 세팅 후 재개.
- **MORAI-003 초안**: 001 회신 흐름에 병행 예정.
- **full_autonomy_analysis.md**: 미업로드(배치 대기 — 문서지도 표기 완료).

## 신규: 이슈 / 결정 / 벤더 커뮤니케이션 (TSV 반영 여부 체크)
- 이슈: 신규 없음 (AVS-007/006 기존; 진단으로 특성화 심화). issues.tsv 신규행 없음.
- 결정: ADR 신규 확정 없음(ADR-009/010 "예정" 유지). decisions.tsv 변경 없음.
- 벤더: `vendor_comms.tsv` 신설 + MORAI-001(DRAFT)·002(DRAFT)·003(TODO_DRAFT), append-only AMEND 행으로 상태 전이 기록. **TSV 반영 ✓**

## 교훈 (CLAUDE.md 규칙 승격 후보 표시)
이번 세션 확립된 규약 3건 — **CLAUDE.md 승격 완료** (2026-07-04_session_2에서 3건 모두 승격, CLAUDE.md 운영 규칙에 반영):
- ① **vendor_comms append-only 전이 행**: 상태 전이는 in-place 편집이 아니라 `AMEND:` 접두 새 행 append(동일 comm_id 최신행 유효). → 승격 완료
- ② **INTERNAL 주석**: OUTBOX 초안의 발송 금지 내용은 `<!-- INTERNAL -->`로 감싸고 SENT 동결 시 제거. → 승격 완료
- ③ **evidence 동결**: 문의 인용 증거는 발송 전 `vendor/<사>/evidence/`에 사본 동결(대외 근거는 git 관리 영역). → 승격 완료
- (기존 디버깅 규칙 "실물 먼저·불일치 선검출"도 이번 세션에서 재차 유효 — 배치 전제 M1~M4 불일치를 실행 전 포착.)

## 다음 세션 필수 작업 (PROJECT_STATUS 7장에 반영했는지 체크) — 반영 ✓
1. MORAI-001/002 발송 (N회 기입 후) → SENT 동결 + vendor_comms SENT 행.
2. SIM-불필요 트랙 착수(eventlog/statelog 스키마 인벤토리 → parser 골격).
3. (PC 접근 시) D1~D5 진단 완주 → AVS-007 귀속 확정.
4. ADR-009(E안) 등재 + 설계노트 erratum.
5. 위 규약 3건 CLAUDE.md 승격 여부 결정.
